package musicFormats;

public class WAV extends AbstractTrack {

    public WAV(String title, double size, double lenght, int number, String genre, String artist, String album) {
        super(title, size, lenght, number, genre, artist, album);
    }
}
